Anwar Lebanon — clean menu/UI package

Replace ONLY these four files in the GitHub Pages repository:
1) index.html
2) kiosk-app.js
3) firebase-app.js
4) service-worker.js

This package:
- restores the original menu card rendering/behavior;
- keeps the homepage hero static and independent from the menu Hero settings;
- cache-busts the JS/Firebase modules;
- replaces the stale-cache behavior with a versioned network-first service worker.

Do not upload backup files from previous packages.
